variable "bucketname" {
    type = string
    description = "it is a name of the buckerr"
    default = "dinesh-bucket-devops"    #the bucket name will be given in  the terraform.tfvars . we can also pass here
  
}